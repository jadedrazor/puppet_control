---
version: 4
datadir: hieradata
hierarchy:
  - name: "virtual/%{facts.virtual}"
    backend: yaml

  - name: "common"
    backend: yaml

  - name: "dev"
    backend: yaml

  - name: "%{::ee_env}"
    backend: yaml
  - name: "%{::ee_role}"
    backend: yaml
  - name: "%{::ee_project}"
    backend: yaml
